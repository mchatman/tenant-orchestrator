name: tenant-provisioner
region: nyc3

services:
- name: tenant-provisioner
  git:
    branch: main
    repo_clone_url: https://github.com/mchatman/tenant-provisioner.git
  run_command: ./tenant-provisioner
  dockerfile_path: Dockerfile
  instance_count: 1
  instance_size_slug: basic-xs
  http_port: 8080

  envs:
  - key: PORT
    value: "8080"